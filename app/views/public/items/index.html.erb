<div class="container mt-5 ">
  <div class="row">
    <div class="col-md-2 mx-auto">
     <!--%= render 'public/homes/list_search', genres: @genres %>-->
    </div>
    <div class="col-md-10 backimage-text">
       <h3><strong>商品一覧 (全<%= @items.count %>件)</strong></h3>
       <div class="row mt-4">
        <div class="col-md-3">
       <div>
         <p>
           <%= link_to item_path do %>
            <div class="image-box">
             <%= image_tag @item.image, style: "max-width: 200px;" %>
            </div>
           <% end %>
         </p>
    <p><strong><%= f.name %></strong>（<%= f.sale_status %>）</p>
    <p><strong>￥<%= f.price_without_tax.to_s(:delimited) %></strong></p>
  </div>
 </div>
        <div class="d-flex justify-content-center mt-4"><%= paginate @items %></div>
       </div>
    </div>
  </div>
</div>